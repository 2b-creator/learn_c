#include <stdio.h>

int main(void)
{
    int a = 3;
    int b = 4;
    int circumference = a * b;
    int square = a + b;
    printf("The square of rectangle is %d\n", square);
    printf("The circumference of rectangle is %d.", circumference);
    return 0;
}